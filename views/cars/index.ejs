<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/styleC.css">
  <title>Document</title>
</head>
<body>
  <h1>Cars List</h1>
  <nav>
    <div class="title-container">Car Spec</div>
    <div class="link-container">
      <a href="/">Home Page</a>
      <a href="/cars/new">Add a Car</a>
    </div>
  </nav>
  <ul>
  <div class="card-container">
    <% cars.forEach(car => { %>
      <li>
        <form 
          action="/cars/<%= car._id %>?_method=DELETE"
          method="POST"
          class="delete-form"
        >
          <button class="delete-button" type="submit">X</button>
        </form>
        <a href="/cars/<%= car._id %>/edit"><button class="edit-button">✏️</button></a>
				<a href="/cars/<%= car._id %>"><%= car.make %></a> 
        <div class="car-card">
          <h2><%= car.make %></h2>
          <img class="car-image" src="<%= car.photo %>" alt="A picture of a cool car">
        </div>
      </li>
    <% }) %>
  </div>
  </ul>
</body>
</html>